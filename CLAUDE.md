@agents.md
@docs/agents/context-loading-policy.md

## Claude Code

- 공통 규칙 원본은 `agents.md` 와 `docs/agents/*` 이다.
- 이 파일은 Claude Code 전용 진입점만 제공하고, 규칙 자체는 중복 정의하지 않는다.
- 경로별 세부 규칙은 `.claude/rules/`, 반복 작업은 `.claude/commands/`, 역할 분리는 `.claude/agents/` 를 따른다.
