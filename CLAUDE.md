# Claude Code Instructions

@AGENTS.md

`AGENTS.md`가 이 저장소의 공통 에이전트 지침 정본이다. 작업 전에 전체를 읽고, 변경 표면에
해당하는 owner 문서만 추가로 읽는다. 프로젝트 skill의 정본은 `.agents/skills/`이며
`.claude/skills` symlink를 통해 같은 skill을 자동 발견한다. 이 파일에 규칙이나 skill 사본을
만들지 않는다. 충돌할 경우 `AGENTS.md`가 우선한다.
