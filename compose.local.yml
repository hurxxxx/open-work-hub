name: open-work-hub-local

services:
  redis:
    image: redis:7
    container_name: open-work-hub-local-redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "127.0.0.1:${OPEN_WORK_HUB_LOCAL_REDIS_PORT:-56381}:6379"
    volumes:
      - open-work-hub-local-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

  opensearch:
    build:
      context: .
      dockerfile: ops/opensearch/Dockerfile
    image: open-work-hub-opensearch:3.3.2-nori
    container_name: open-work-hub-local-opensearch
    restart: unless-stopped
    environment:
      discovery.type: single-node
      DISABLE_SECURITY_PLUGIN: "true"
      OPENSEARCH_JAVA_OPTS: "-Xms512m -Xmx512m"
    ports:
      - "127.0.0.1:${OPEN_WORK_HUB_LOCAL_OPENSEARCH_PORT:-59220}:9200"
      - "127.0.0.1:${OPEN_WORK_HUB_LOCAL_OPENSEARCH_PERF_PORT:-59620}:9600"
    volumes:
      - open-work-hub-local-opensearch-data:/usr/share/opensearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:9200 >/dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20

  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z@sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e
    container_name: open-work-hub-local-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${OPEN_WORK_HUB_LOCAL_MINIO_ROOT_USER:-open_work_hub_local_minio}
      MINIO_ROOT_PASSWORD: ${OPEN_WORK_HUB_LOCAL_MINIO_ROOT_PASSWORD:-open_work_hub_local_minio}
    command: server /data --console-address :9001
    ports:
      - "127.0.0.1:${OPEN_WORK_HUB_LOCAL_MINIO_PORT:-59020}:9000"
      - "127.0.0.1:${OPEN_WORK_HUB_LOCAL_MINIO_CONSOLE_PORT:-59021}:9001"
    volumes:
      - open-work-hub-local-minio-data:/data

  qdrant:
    image: qdrant/qdrant:v1.17.1
    container_name: open-work-hub-local-qdrant
    restart: unless-stopped
    environment:
      QDRANT__SERVICE__API_KEY: ${OPEN_WORK_HUB_LOCAL_QDRANT_API_KEY:-open_work_hub_local_qdrant}
    ports:
      - "127.0.0.1:${OPEN_WORK_HUB_LOCAL_QDRANT_PORT:-16335}:6333"
      - "127.0.0.1:${OPEN_WORK_HUB_LOCAL_QDRANT_GRPC_PORT:-16336}:6334"
    volumes:
      - open-work-hub-local-qdrant-data:/qdrant/storage

volumes:
  open-work-hub-local-redis-data:
    name: open-work-hub-local-redis-data
  open-work-hub-local-opensearch-data:
    name: open-work-hub-local-opensearch-data
  open-work-hub-local-minio-data:
    name: open-work-hub-local-minio-data
  open-work-hub-local-qdrant-data:
    name: open-work-hub-local-qdrant-data
