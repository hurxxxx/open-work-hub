map $http_upgrade $connection_upgrade {
  default upgrade;
  '' close;
}

upstream doowon_api_cluster {
  server dev-host:8001;
  server dev-host:8002;
  server dev-host:8003;
  server dev-host:8004;
  server dev-host:8005;
  server dev-host:8006;
  server dev-host:8007;
  server dev-host:8008;
}

server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass http://doowon_api_cluster;
    proxy_http_version 1.1;
    proxy_next_upstream error timeout http_502 http_503 http_504;
    proxy_next_upstream_tries 3;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;
    proxy_buffering off;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
