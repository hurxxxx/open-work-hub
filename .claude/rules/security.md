# Security Rules

- `legacy_ai_portal_prototype/` 내부 파일은 수정하지 않는다.
- 사용자의 별도 지시 없이 `git commit`, `git push`, destructive git 명령을 실행하지 않는다.
- 권한 없는 문서나 PLM 데이터를 retrieval 단계부터 제거한다.
- 민감정보, 실제 운영 계정, 토큰 값은 문서나 예시에 하드코딩하지 않는다.
