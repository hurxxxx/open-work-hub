# Shared UI Rules

- `packages/ui` 는 웹 UI 공통 컴포넌트의 단일 진실원본이다.
- 앱이나 도메인에서 새 UI를 만들기 전에 먼저 여기 구현이 있는지 확인한다.
- 공통화 가능한 버튼, 배지, 툴바, 필터, 테이블, 드로어, 토스트, 차트는 앱 안에서 직접 재구현하지 않는다.
- 외부 UI 라이브러리는 `packages/ui` 내부 wrapper를 통해서만 사용한다.
- `apps/web` 는 이 패키지를 조합하고, 페이지별 예외와 레이아웃만 직접 소유한다.
