# 09. API와 웹 통신 — HTTP, REST, WebSocket, SSE

> **한 줄 요약.** API는 "프로그램끼리 대화할 때 쓰는 약속된 언어"다. 가장 흔한 HTTP 기반 REST API, 양방향 실시간용 WebSocket, 서버가 일방적으로 밀어넣는 SSE를 알면 이 프로젝트의 통신 대부분이 설명된다.

---

## 1. "API"라는 말을 정리하자

API = **Application Programming Interface**. 직역하면 "응용 프로그램 인터페이스". 의역하면 **"프로그램이 다른 프로그램에게 기능을 빌려주는 창구"** 입니다.

비유: 식당에서 손님(클라이언트)은 주방(서버)에 직접 들어가지 않습니다. **메뉴판**에 적힌 요리만 주문할 수 있습니다. 메뉴판이 바로 API입니다. 주방은 바뀔 수 있지만 메뉴판의 약속(이름, 가격, 구성)만 지키면 손님은 그대로 주문합니다.

우리 프로젝트에서 "API"라고 하면 대부분 **"웹 브라우저가 우리 백엔드 서버에게 보내는 HTTP 요청/응답"** 을 뜻합니다.

### 1.1 일상 비유 더

- **콜센터 ARS**: "1번 누르면 상담, 2번 누르면 요금조회". 내부 상담 시스템은 수시로 바뀌어도 ARS 메뉴(API 명세)만 유지되면 고객은 영향 없다.
- **은행 창구 양식지**: 송금·예금·해지 각 양식지(엔드포인트)가 따로 있고, 필수 기입란(파라미터)이 정해져 있다. 양식이 틀리면 창구(서버)가 돌려보낸다.

### 1.2 ⚠️ 흔한 오해

- **오해:** API는 개발자끼리 부르는 신기한 기술 용어다. **실제:** API는 **약속의 이름**일 뿐이다. 엑셀 함수 `=SUM(A1:A10)`도 일종의 API(함수 호출 규약)다.
- **오해:** API = 외부에 공개된 서비스만 가리킨다. **실제:** 같은 회사 내 두 앱 사이의 대화도 API다. **공개·비공개는 별개 축**이다.

---

## 2. HTTP — 웹의 기본 규약

HTTP(HyperText Transfer Protocol)는 1989년 팀 버너스 리가 설계한 웹의 기본 프로토콜입니다. 오늘날 모든 웹 서비스가 HTTP(또는 그 암호화 버전 HTTPS)를 씁니다.

### 2.1 요청-응답 모델

```
[클라이언트] --- HTTP 요청 --->  [서버]
             <--- HTTP 응답 ---
```

한 번의 대화는 요청 1개, 응답 1개로 끝납니다. 이를 **stateless(상태 없음)** 이라 합니다.

### 2.2 요청의 구성

```
POST /ai/chat HTTP/1.1
Host: portal.doowon.co
Authorization: Bearer eyJhbGciOi...
Content-Type: application/json

{"message": "요약해 줘", "conversationId": "abc"}
```

- **메서드(Method)**: GET, POST, PUT, PATCH, DELETE
- **경로(Path)**: `/ai/chat`
- **헤더(Headers)**: 메타 정보 (인증 토큰, 콘텐츠 타입 등)
- **본문(Body)**: 보내는 데이터

### 2.3 응답의 구성

```
HTTP/1.1 200 OK
Content-Type: application/json

{"reply": "요약입니다: ..."}
```

- **상태 코드(Status Code)**: 200(성공), 400(잘못된 요청), 401(인증 실패), 403(권한 없음), 404(없음), 500(서버 오류)
- 자주 보는 상태 코드만 외워두면 디버깅이 쉽습니다.

### 2.4 ⚠️ 흔한 오해

- **오해:** HTTP와 HTTPS는 거의 같다. **실제:** HTTPS는 암호화(TLS) 계층이 끼어 있어 **중간 도청과 변조를 차단**한다. 사내용이어도 HTTPS가 기본이어야 한다.
- **오해:** 404가 뜨면 무조건 서버 고장. **실제:** 404는 "그 주소에 해당하는 리소스가 없다"는 **정상 응답**이기도 하다. 오타·권한 설정 문제인 경우가 많다.
- **오해:** stateless라서 서버는 사용자 정보를 기억 못 한다. **실제:** **요청마다 토큰/쿠키**로 신원을 알려주면 된다. stateless는 "매 요청이 독립적"이라는 뜻이지, "영영 모른다"는 뜻이 아니다.

### 2.5 🏢 업무 시나리오

**케이스 — 401과 403 구분이 UX를 바꾼다.**
사용자가 회의록 페이지를 열었는데 서버가 **401**을 보냈다면 "**로그인 먼저**" 화면을 띄워야 한다. 서버가 **403**을 보냈다면 "이 워크스페이스 멤버가 아닙니다"라는 안내를 보여야 한다. 코드 하나 차이가 사용자 경험을 크게 바꾼다.

---

## 3. REST — 가장 널리 쓰이는 API 스타일

REST(REpresentational State Transfer)는 2000년 Roy Fielding의 박사 논문에서 등장한 HTTP 기반 API 설계 **스타일** 입니다.

### 3.1 핵심 규칙

- 모든 것은 **리소스(resource)** 다. `users`, `meetings`, `documents`
- 리소스에 대한 행위는 **HTTP 메서드** 로 표현한다.

| 행위 | 메서드 | 경로 예 |
|---|---|---|
| 목록 조회 | GET | `/users` |
| 단건 조회 | GET | `/users/123` |
| 생성 | POST | `/users` |
| 전체 수정 | PUT | `/users/123` |
| 부분 수정 | PATCH | `/users/123` |
| 삭제 | DELETE | `/users/123` |

### 3.2 왜 REST가 표준이 됐나

- HTTP의 기본 어휘(메서드, 상태코드)를 그대로 씀 → **학습 비용 낮음**.
- URL만 봐도 용도 짐작 가능 → **협업 편함**.
- 인프라(캐시, 게이트웨이, 로드밸런서)가 이미 HTTP에 맞춰져 있음.

### 3.3 REST의 한계

- **여러 리소스를 한 번에 조회** 하는 게 비효율적 (여러 번 요청해야 함).
- **실시간 푸시** 에 적합하지 않다.
- 명세가 말랑말랑해서 팀마다 스타일이 갈린다.

### 3.4 REST의 대안

- **GraphQL** (2015, Facebook): 클라이언트가 필요한 필드만 쿼리. 복잡한 UI에 좋음. 단점: 서버 구현이 복잡.
- **gRPC** (2015, Google): 고성능 바이너리 프로토콜. 서비스 간 통신에 좋음. 브라우저 직접 사용은 제약.
- **tRPC** (TypeScript 전용): 프런트와 백이 같은 타입을 공유하는 RPC. 모노레포 + 풀 TS에 최적.

우리 프로젝트는 **REST(JSON over HTTP)** 를 선택. 이유:

- 프런트(TS)와 백(Python) 언어가 달라 gRPC·tRPC보다 REST가 자연스럽다.
- FastAPI가 REST + 자동 OpenAPI 문서를 기본 제공.
- 읽기 쿼리가 대체로 단순해 GraphQL의 장점이 과잉.

### 3.5 ⚠️ 흔한 오해

- **오해:** REST만이 "올바른" API 설계다. **실제:** REST는 **스타일**일 뿐이다. 실시간 협업·AI 스트리밍 같은 상황에서는 WebSocket·SSE가 적절하다.
- **오해:** URL에 동사를 쓰면 안 된다 (`/getUsers` 금지). **실제:** REST 관점에서는 맞지만, 현실에서는 **행위 기반 엔드포인트**(예: `/meetings/123/cancel`)가 더 자연스러운 경우도 많다. 규칙은 도구일 뿐.

---

## 4. OpenAPI (Swagger) — API 명세의 표준

REST API의 **메뉴판을 표준 포맷으로 적어 둔 것** 이 OpenAPI입니다(구명칭 Swagger).

**FastAPI는 타입 정의만 해 두면 OpenAPI를 자동 생성** 합니다. `/docs` 경로로 들어가면 브라우저에서 바로 테스트 가능한 UI가 나옵니다.

```
http://localhost:8000/docs
```

이게 정말 강력합니다. 비개발자도 이 페이지만 열면 "어떤 API가 있고 뭘 받고 뭘 돌려주는지" 즉시 확인할 수 있습니다. 커뮤니케이션 비용이 크게 줍니다.

### 4.1 🛠️ 5분 실습

- 로컬에서 백엔드가 떠 있다면 브라우저로 `http://localhost:8000/docs` 에 접속.
- 엔드포인트 한 개를 펼쳐서 **"Try it out"** 버튼을 눌러 직접 호출해 본다.
- 응답 JSON이 화면에 뜨는 순간, API가 "창구에서 서류 받는 창구" 같다는 비유가 체감된다.

### 4.2 ⚠️ 흔한 오해

- **오해:** OpenAPI 문서는 별도로 작성해야 한다. **실제:** FastAPI는 **타입 힌트만 쓰면 자동 생성**된다. "문서가 항상 최신 상태"가 보장된다.

---

## 5. WebSocket — 양방향 실시간 통신

HTTP는 "한 번 묻고 한 번 답"입니다. 여러 명이 동시에 문서를 편집하려면 어떻게 할까요? 서버가 변경을 **능동적으로** 클라이언트에 밀어넣어야 합니다. 이걸 HTTP만으로 하려면 무식하게 계속 요청을 보내는 **폴링(polling)** 이 유일한 방법인데, 매우 비효율적입니다.

**WebSocket** 은 이 문제를 해결합니다. 한 번 "연결"을 맺으면, **양쪽이 언제든 메시지를 보낼 수 있는 파이프** 가 생깁니다. 브라우저와 서버가 대화하듯 주고받습니다.

### 5.1 우리 프로젝트의 WebSocket

- **실시간 협업 에디터(BlockNote + Yjs)** — 문서 편집 변경을 실시간 전파.
- 서버 측 구현: `ypy-websocket` (Python Yjs 구현) + FastAPI의 WebSocket 지원.
- 클라이언트: `y-websocket` JS 라이브러리.

자세한 것은 26장에서 다룹니다.

---

## 6. SSE (Server-Sent Events) — 서버에서 클라이언트로의 일방향 스트림

WebSocket이 **양방향** 이라면, SSE는 **서버→클라이언트 일방향** 입니다. 그리고 HTTP 위에서 동작하므로 WebSocket보다 가볍고 구현이 간단합니다.

### 6.1 언제 쓰는가

- AI 응답을 **한 글자씩** 흘려보낼 때 ← **이 프로젝트의 메인 용도**
- 서버의 진행률 업데이트(예: 파일 업로드, 트랜스크립션 진행률)
- 대시보드의 실시간 수치 업데이트

### 6.2 우리 프로젝트의 SSE

AI 채팅이 핵심 예입니다.

```
클라이언트 → POST /ai/chat  (요청)
서버 ← 응답 시작, Content-Type: text/event-stream
서버 → event: token  data: "안"
서버 → event: token  data: "녕"
서버 → event: token  data: "하세요"
서버 → event: tool_call  data: {...}
서버 → event: done
```

한 번의 요청에 대해 서버가 **계속 이벤트를 흘려보내고**, 브라우저는 그걸 받아 화면에 타이핑하듯 표시합니다.

서버 측 라이브러리: **`sse-starlette`** (FastAPI용).
클라이언트 측: **`eventsource-parser`** (프런트 의존성에 등록돼 있음).

### 6.3 WebSocket vs SSE — 언제 뭐를?

| 상황 | 선택 |
|---|---|
| 둘 다 메시지 주고받음 (채팅, 게임, 협업 에디터) | **WebSocket** |
| 서버만 밀어넣음 (AI 스트리밍, 알림, 진행률) | **SSE** |
| 단순 요청/응답 | **HTTP(REST)** |

### 6.4 일상 비유

- **전화 통화(WebSocket)** vs **라디오 방송(SSE)** vs **우편 편지(REST)**. 전화는 양방향, 방송은 발신만, 편지는 한 번에 하나씩.
- **사내 메신저**: 두 사람이 동시에 타이핑(WebSocket). 관리자 공지는 한 방향(SSE). 승인 요청서는 한 번 주고받기(REST).

### 6.5 ⚠️ 흔한 오해

- **오해:** WebSocket이 "더 좋은" 기술이니 무조건 쓴다. **실제:** WebSocket은 **상태를 유지**해야 해서 서버 부담과 운영 복잡도가 크다. 일방향이면 SSE로 충분.
- **오해:** SSE는 구식이다. **실제:** 2024년 이후 **AI 스트리밍의 사실상 표준**으로 부활했다. OpenAI/Anthropic API도 SSE로 토큰을 흘린다.

---

## 7. RPC와 그 바리에이션

RPC(Remote Procedure Call) = **"원격 함수 호출"**. 네트워크 너머의 서버에 있는 함수를 내가 내 PC에서 부르는 것처럼 쓰는 방식.

### 7.1 JSON-RPC

단순하게 JSON으로 "이 함수를 이 인자로 불러줘"라고 보내는 방식.

```
→ {"method": "calendar.create", "params": {...}, "id": 1}
← {"result": {...}, "id": 1}
```

**MCP(Model Context Protocol)** 가 JSON-RPC 2.0을 베이스로 합니다. 32장에서 자세히.

### 7.2 gRPC / Protocol Buffers

Google이 만든 고성능 RPC. 텍스트가 아닌 **이진(binary)** 포맷을 씀. 마이크로서비스 간 내부 통신에 인기.

우리 프로젝트에는 **쓰지 않습니다**. 이유: 브라우저에서 gRPC 직접 사용이 제약 많고, 내부 서비스 수가 적다.

---

## 8. 인증(Authentication) 과 권한(Authorization)

API 보안에는 두 개념이 붙어 다닙니다.

- **Authentication(인증)** = **"당신이 누구인가"** 확인.
- **Authorization(권한)** = **"당신이 이 일을 할 수 있는가"** 확인.

### 8.1 흔한 토큰 방식

- **Session Cookie** (전통적): 서버가 상태를 보관, 브라우저에는 쿠키.
- **JWT** (Stateless): 서명된 토큰 안에 사용자 정보가 담겨 있음. 서버는 서명만 검증.
- **OAuth 2.0 + OpenID Connect**: 타사 로그인(Google, Microsoft 등).

### 8.2 이 프로젝트

- 현재는 **ID/PW + 토큰** 기반 (제품 문서 `ai_portal.md`: "sso 및 ad 는 복잡하니깐 하니말고 id/pw 기반으로 우선 적용 향후 sso/인증은 전사 도입 시에 고려").
- 관리자/워크스페이스 멤버십에 따른 **권한 가드**가 백엔드 `require_workspace_membership` 같은 의존성으로 걸려 있음.

### 8.3 일상 비유

- **회사 출입증**: 배지를 찍는 순간(인증) "이 사람이 직원인지"는 확인된다. 하지만 임원실 문이 열리려면 **임원 권한**(권한)이 별도로 필요하다.
- **공연장 입장**: 티켓이 있으면 장내 입장(인증). VIP석은 좌석 번호가 맞아야 앉을 수 있다(권한).

### 8.4 ⚠️ 흔한 오해

- **오해:** 로그인만 돼 있으면 모든 걸 볼 수 있다. **실제:** 로그인은 **1단계**일 뿐, 매 요청마다 **"이 리소스에 접근할 권한이 있는가"** 가 별도로 체크돼야 한다. 이것이 OWASP Top 1인 **Broken Access Control** 문제의 핵심.
- **오해:** JWT는 "로그아웃"이 어렵다. **실제:** JWT는 서버에 상태를 안 두기에 **즉시 무효화가 원천적으로 까다롭다**. 그래서 토큰 만료 시간을 짧게 두거나 블랙리스트를 병용한다.

---

## 9. API 설계의 좋은 습관

### 9.1 버전 관리

`/api/v1/users` 처럼 버전을 경로에 넣으면, 나중에 `v2`를 만들어도 기존 클라이언트가 깨지지 않는다.

### 9.2 에러는 일관된 포맷으로

```json
{
  "error": "invalid_credentials",
  "message": "이메일 또는 비밀번호가 일치하지 않습니다.",
  "details": { ... }
}
```

### 9.3 idempotent(멱등)한 메서드

GET, PUT, DELETE는 **여러 번 호출해도 같은 결과** 여야 함. POST는 예외.

### 9.4 페이지네이션

목록 조회는 반드시 페이지네이션. `?page=2&size=20` 또는 **커서 기반** (`?cursor=abc`).

### 9.5 Rate limiting

악성 요청·과도한 호출을 막기 위한 제한. 특히 AI 엔드포인트는 비용이 드니 필수.

---

## 10. 핵심 요약

- API는 프로그램 간의 **메뉴판**. 가장 보편적인 기반은 **HTTP**.
- **REST**는 HTTP 메서드로 리소스를 조작하는 스타일. 우리 프로젝트의 메인.
- **OpenAPI**는 REST API의 표준 명세. FastAPI가 자동 생성.
- **WebSocket**은 양방향 실시간. 우리는 BlockNote 협업에 사용.
- **SSE**는 서버→클라이언트 일방향. 우리는 AI 스트리밍에 사용.
- 인증/권한은 API의 보안 기둥. 현재는 ID/PW + 토큰.

---

## 11. 이해도 체크

1. HTTP가 "stateless"라는 말의 뜻을 식당 비유로 설명하세요.
2. 401과 403의 차이는? 사용자에게 각각 어떤 안내를 보여야 하나요?
3. REST 메서드 4개(GET, POST, PUT, DELETE)의 쓰임을 회의실 리소스로 예시해 보세요.
4. WebSocket 대신 SSE를 쓰면 좋은 상황 하나를 들어 보세요.
5. OpenAPI 자동 생성이 팀 커뮤니케이션에 어떤 이익을 주는지 설명하세요.
6. "멱등(idempotent)"이란 무엇이며, 왜 GET·DELETE·PUT은 멱등이어야 하는가?
7. **인증과 권한의 차이**를 본인 회사의 출입 통제 비유로 설명해 보세요.
